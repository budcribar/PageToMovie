@page "/cut"
@inject NavigationManager Nav

<p class="text-muted p-3 mb-0">Opening finish…</p>

@code {
    protected override void OnInitialized()
    {
        Nav.NavigateTo("/review?tab=finish", replace: true);
    }
}
